using System.Diagnostics;

class test
{
    Fraction fraction = new Fraction();
    public int Replace(int newTop)
    {
        int top;
        top = fraction.GetTop();
        newTop = 5;
        top = newTop;
        return top;

        
    }

}